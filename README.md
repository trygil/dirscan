This script scan path and count on how many files inside the given path that have the same content.
Path must be defined in config file (`config.yml`).  


For example:
```
paths:
- /first/dir
- /another/dir
```

Run script  
`ruby scan.rb`
